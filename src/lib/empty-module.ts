// Stub for optional native/node-only deps (sharp, onnxruntime-node, canvas,
// encoding) pulled in transitively by browser-only ML libraries. They have
// no browser build and are never actually called from client code here.
const emptyModule = {};
export default emptyModule;

